{
  "compilerOptions": {
    "target": "es2020",                       /* Версия JS в которую компилируем */
    "module": "commonjs",                     /* В какую модульную систему производить компиляцию */
    "rootDir": "./src",                       /* Определяет, где компилятор ищет файлы TypeScript */
    "outDir": "./",                           /* Определения папки, в которую TypeScript будет компилировать */
    "esModuleInterop": true,                  /* Позволяет использовать синтаксис ES6 для импорта модулей CommonJS */
    "forceConsistentCasingInFileNames": true, /* Контролирует, чтобы регистр символов в имени файла совпадал при импорте. */
    "strict": true,                           /* Строгий режим JS */
  },
"include": [ "src/**/*.ts" ],               /* Указывает компилятору на подпапки и файлы c TypeScript */
  "exclude": ["node_modules"],                /* Исключение для компиляции */ 
}